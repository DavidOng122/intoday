[project]
name = "memotask-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "markitdown>=0.1.5",
    "pypdf>=6.10.2",
]
