// Re-export barrel — kept for backward compatibility during migration.
// New code must import from 'features/inbox' instead.
export {
  COLLECTION_STATES,
  normalizeCollectionState,
} from '../features/inbox/model/collectionState.js';
