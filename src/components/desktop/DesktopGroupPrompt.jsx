// Re-export barrel — kept for backward compatibility during migration.
// New code must import from 'features/pack' instead.
import PackPrompt from '../../features/pack/components/PackPrompt';
export default PackPrompt;
