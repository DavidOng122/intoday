// Re-export barrel — kept for backward compatibility during migration.
// New code must import from 'features/capture' instead.
export { useDesktopCapture as useDesktopUploads } from '../features/capture/hooks/useDesktopCapture';
